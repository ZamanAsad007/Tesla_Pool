{
  "name": "tesla-pool",
  "version": "1.0.0",
  "private": true,
  "description": "Dhaka Tesla Pool - Battery-powered rickshaw ride sharing in Dhaka",
  "scripts": {
    "build": "npm run build --prefix api && npm run build --prefix web",
    "test": "npm run test --prefix api && npm run test --prefix web",
    "test:api": "npm run test --prefix api",
    "test:web": "npm run test --prefix web",
    "dev:api": "npm run dev --prefix api",
    "dev:web": "npm run dev --prefix web"
  }
}
